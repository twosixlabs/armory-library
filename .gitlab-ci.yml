image: "python3.10"

before_script:
  - python --version
  - pip install .[developer,pytorch]

stages:
  - Static Analysis

flake8:
  stage: Static Analysis
  script:
    - black
    - isort
    - python -m flake8 --config=.flake8
